<Window x:Class="Piterads.Window4"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Админ-панель" Height="650" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#696969">
    <Window.Resources>
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#696969"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
        </Style>

        <Style x:Key="NavButtonActiveStyle" TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
            <Setter Property="Background" Value="#F8C789"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F8C789"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="10,0,0,0"/>
        </Style>

        <Style x:Key="DataGridColumnHeaderStyle" TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="Background" Value="#D2B48C"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="DataGridCellStyle" TargetType="{x:Type DataGridCell}">
            <Setter Property="Background" Value="#696969"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="BorderBrush" Value="#888888"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#D2B48C"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DataGridRowStyle" TargetType="{x:Type DataGridRow}">
            <Setter Property="Background" Value="#696969"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                    <Setter Property="Background" Value="#555555"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#F8C789">
            <Grid Margin="15,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Piterads"
                           VerticalAlignment="Center"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="Black"/>

                <TextBlock Grid.Column="1" Text="Админ-панель"
                           VerticalAlignment="Center"
                           FontSize="16"
                           Foreground="Black"
                           Margin="0,0,15,0"/>

                <Button Grid.Column="2" Content="Выход"
                        Background="#696969"
                        Foreground="White"
                        FontWeight="SemiBold"
                        Padding="10,2"
                        BorderThickness="0"
                        Margin="0,12,0,12" Click="Button_Click"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="10,10,10,10">
            <StackPanel Orientation="Horizontal">
                <Button Content="Клиенты" Style="{StaticResource NavButtonActiveStyle}"/>
                <Button Content="Продукты" Style="{StaticResource NavButtonStyle}"/>
                <Button Content="Проекты" Style="{StaticResource NavButtonStyle}"/>
                <Button Content="Тип проекта" Style="{StaticResource NavButtonStyle}"/>
            </StackPanel>

            <Grid Margin="0,15,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0" HorizontalAlignment="Left" Width="250">
                    <TextBox Height="35" Background="White" BorderThickness="0" Padding="35,0,5,0"/>
                    <Button Width="35" Height="35" Background="White" BorderThickness="0" HorizontalAlignment="Left" Padding="0">
                        <TextBlock Text="🔍" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Button>
                </Grid>

                <Button Grid.Column="1" Content="Добавить" Style="{StaticResource ActionButtonStyle}" />
                <Button Grid.Column="2" Content="Редактировать" Style="{StaticResource ActionButtonStyle}" />
                <Button Grid.Column="3" Content="Удалить" Style="{StaticResource ActionButtonStyle}" />
            </Grid>
        </StackPanel>

        <Border Grid.Row="2" Margin="10,0,10,10" Background="White" CornerRadius="5">
            <DataGrid x:Name="ClientsDataGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      Background="#F8C789"
                      HeadersVisibility="Column"
                      RowHeaderWidth="0"
                      BorderThickness="0"
                      CellStyle="{StaticResource DataGridCellStyle}"
                      ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                      RowStyle="{StaticResource DataGridRowStyle}">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="User_Id" Binding="{Binding UserId}" Width="50"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="1.5*"/>
                    <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="*"/>
                    <DataGridTextColumn Header="Login" Binding="{Binding Login}" Width="*"/>
                    <DataGridTextColumn Header="Password" Binding="{Binding Password}" Width="*"/>
                    <DataGridTextColumn Header="Role_id" Binding="{Binding RoleId}" Width="50"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Window>
